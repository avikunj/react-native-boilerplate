import * as React from 'react';
import { Appbar } from 'react-native-paper';

const AppHeaderBackAction = () => (
    <Appbar.Header>
      <Appbar.BackAction onPress={() => {}} />
    </Appbar.Header>
);

export default AppHeaderBackAction;