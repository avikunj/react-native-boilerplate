import React from 'react';
import AppText from '../../components/text';

const OpenSourceLibrariesScreen = () => {
  return <AppText value={'OpenSourceLibraries Screen'} />;
};

export default OpenSourceLibrariesScreen;
